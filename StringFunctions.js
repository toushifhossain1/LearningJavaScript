// fName =

// String functions

// document.write("Length of fname Toushif is: " + fName.length)

// to take an input from user: use the function "prompt()"'

yourFirstName = prompt("Enter your first name: ")
yourLastName = prompt("Enter your Last name: ")

document.write(yourFirstName + yourLastName)
fullName = yourFirstName.toUpperCase() + yourLastName.toUpperCase()

document.write(fullName)
document.write(fullName.length)

//document.write("your first name is: " + yourFirstName + " And it's lenght is : " + yourFirstName.length)

// to find the character at any place use the function charAt()
